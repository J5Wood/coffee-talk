<h1>Brands</h1>
<br>
<%= link_to "Add New Brand", new_brand_path %>
<br>
<br>
<ul>
    <% @brands.each do |brand| %>
        <li><%= link_to brand.name, brand_path(brand) %> - <%= render partial: "brands/stars", locals: {rating: brand.average_rating} %></li>
        <br>
    <% end %>
</ul>